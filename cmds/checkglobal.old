module.exports = {
    name: "checkglobal",
    type: "messageCreate",
    code: `
$onlyForUsers[;$botOwnerID]

Checking command registration...

Guild Commands in this server: $applicationCommandCount[$guildID;true]
Global Commands: $applicationCommandCount

If guild count is 0, commands are registered globally and will take up to 1 hour to show.
    `
}
